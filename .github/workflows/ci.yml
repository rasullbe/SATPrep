name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore
        working-directory: SATPrep

      - name: Build
        run: dotnet build --no-restore --configuration Release
        working-directory: SATPrep

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: SATPrep/src/frontend/package-lock.json

      - name: Install frontend dependencies
        run: npm ci
        working-directory: SATPrep/src/frontend

      - name: Build frontend
        run: npm run build
        working-directory: SATPrep/src/frontend
